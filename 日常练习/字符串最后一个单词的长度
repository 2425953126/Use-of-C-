#include <iostream>
#include <string>
using namespace std;

int main() 
{
   string a;
   getline(cin,a);
   int length=0;
   for( string::reverse_iterator it=a.rbegin();it!=a.rend();it++)
   {
        if(*it==' ')
            break;
        length++;
   }
   cout<<length;
}