void reverseString(char* s, int sSize)
{
    int begin =0,end=sSize-1;
        while(begin<end)
        {   
            char tmp;
            tmp=s[begin];
            s[begin]=s[end];
            s[end]=tmp;
            begin++;
            end--;
        }
}